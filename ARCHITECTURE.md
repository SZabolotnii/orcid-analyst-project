# ORCID Analyst - Документація архітектури

## Огляд проекту

ORCID Analyst - це сучасний web-застосунок для аналізу публікаційної активності дослідників. Проект побудований на React та Tailwind CSS з використанням Vite як збирача.

## Архітектура

### Frontend Stack

```
┌─────────────────────────────────────────────────┐
│              React Application                  │
├─────────────────────────────────────────────────┤
│  Pages (Home, History)                          │
│  ├─ Components (Analysis, Chat, UI)             │
│  ├─ Hooks (useState, useEffect, useRef)         │
│  └─ Libraries (Recharts, Framer Motion)         │
├─────────────────────────────────────────────────┤
│  Routing (React Router)                         │
├─────────────────────────────────────────────────┤
│  External APIs                                  │
│  └─ ORCID Public API (https://orcid.org)       │
└─────────────────────────────────────────────────┘
```

## Структура файлів

```
orcid-analyst-project/
│
├── 📄 index.html                    # HTML точка входу
├── 📄 package.json                  # Залежності проекту
├── 📄 vite.config.js               # Конфігурація Vite
├── 📄 tailwind.config.js           # Конфігурація Tailwind
├── 📄 postcss.config.js            # PostCSS конфігурація
├── 📄 README.md                    # Документація
├── 📄 .gitignore                   # Git налаштування
│
└── 📁 src/
    │
    ├── 📁 components/              # React компоненти
    │   ├── 📁 analysis/            # Компоненти аналізу
    │   │   ├── OrcidInput.jsx      # Введення ORCID ID
    │   │   ├── PublicationCharts.jsx # Графіки
    │   │   ├── StatsOverview.jsx   # Статистика
    │   │   └── PublicationsList.jsx # Список публікацій
    │   │
    │   ├── 📁 chat/                # Чат компоненти
    │   │   ├── ChatInterface.jsx   # Чат інтерфейс
    │   │   └── MessageBubble.jsx   # Повідомлення
    │   │
    │   └── 📁 ui/                  # UI компоненти
    │       ├── button.jsx          # Button компонент
    │       ├── input.jsx           # Input компонент
    │       ├── card.jsx            # Card компонент
    │       └── index.jsx           # Інші компоненти
    │
    ├── 📁 pages/                   # Сторінки додатку
    │   ├── Home.jsx               # Головна сторінка
    │   └── History.jsx            # Сторінка історії
    │
    ├── 📁 lib/                     # Утилітні функції
    │   └── utils.js               # Допоміжні функції
    │
    ├── 📁 schemas/                 # Схеми даних
    │   ├── OrcidAnalysis.json     # Схема аналізу
    │   └── AnalysisGroup.json     # Схема групи
    │
    ├── 📁 agents/                  # AI агенти
    │   └── orcid_analyst.json     # ORCID аналітик
    │
    ├── Layout.jsx                 # Основний Layout
    ├── main.jsx                   # React точка входу
    └── index.css                  # Глобальні стилі
```

## Основні компоненти

### OrcidInput
- **Функція**: Введення одного ORCID ID або завантаження CSV з кількома ID
- **Особливості**:
  - Валідація формату ORCID (XXXX-XXXX-XXXX-XXXXX)
  - Drag-and-drop для CSV файлів
  - Обробка помилок при невірному форматі

### PublicationCharts
- **Функція**: Візуалізація публікацій
- **Графіки**:
  - Bar Chart: Динаміка по роках
  - Pie Chart: Розподіл за типами
- **Технологія**: Recharts

### ChatInterface
- **Функція**: Взаємодія з AI-аналітиком
- **Особливості**:
  - Real-time повідомлення
  - Markdown підтримка
  - Пропозиції запитань

## Потік даних

```
User Input
    ↓
OrcidInput (валідація)
    ↓
ORCID Public API (fetch)
    ↓
Data Processing (parseWorks)
    ↓
State Management (useState)
    ↓
Component Rendering
    ↓
Display Results (Charts, Stats, List)
```

## API Інтеграція

### ORCID Public API

**Endpoint**: `https://pub.orcid.org/v3.0/{orcid_id}/works`

**Відповідь містить**:
- work-summary (основна інформація про роботу)
- publication-date (дата публікації)
- type (тип публікації)
- external-ids (DOI та інші ідентифікатори)
- journal-title (назва журналу)

**Обробка**:
```javascript
const parseWorks = (data) => {
  return {
    publications: [],      // Список всіх публікацій
    byYear: {},           // Групування по роках
    byType: {}            // Групування по типах
  }
}
```

## Типи публікацій

| Тип | Назва |
|-----|-------|
| journal-article | Статті в журналах |
| book | Книги |
| book-chapter | Розділи книг |
| conference-paper | Конференційні роботи |
| dissertation | Дисертації |
| preprint | Препринти |
| report | Звіти |
| other | Інше |

## Управління станом

### Home Page
```javascript
- activeTab: string           // Активна вкладка
- isLoading: boolean          // Статус завантаження
- analysisResult: object      // Результат аналізу
- groupResult: object         // Результат групи
```

### Chat Interface
```javascript
- messages: array             // Список повідомлень
- input: string               // Введений текст
- isLoading: boolean          // Статус завантаження
```

## Стилізація

### Tailwind CSS
- Утилітарний підхід до CSS
- Custom конфігурація в `tailwind.config.js`
- Темні та світлі кольори

### Палітра кольорів
```
Primary:   Indigo (600, 700)
Secondary: Purple (600)
Success:   Emerald (500)
Warning:   Amber (500)
Error:     Red (500)
```

## Анімації

### Framer Motion
- Fade In/Out анімації
- Slide Up анімації
- Staggered детей анімаціями
- Transitions та delays

## Оптимізація

### Performance
- Lazy loading компонентів
- Memoization де необхідно
- Efficient re-renders
- Code splitting з Vite

### UX/UI
- Responsive дизайн (mobile-first)
- Smooth transitions
- Loading states
- Error handling
- Accessible компоненти

## Розширення

### Можливі улучшення
1. **Backend API**: Заміна fetch на GraphQL
2. **Database**: Зберігання результатів аналізів
3. **Authentication**: User accounts та profiling
4. **Export**: PDF/Excel звіти
5. **Advanced Charts**: D3.js інтеграція
6. **Real-time Sync**: WebSocket підтримка

## Development

### Запуск
```bash
npm install
npm run dev
```

### Build
```bash
npm run build
```

### Preview
```bash
npm run preview
```

## Browser Support

- Chrome (latest)
- Firefox (latest)
- Safari (latest)
- Edge (latest)

## Залежності

### Core
- react@18.2.0
- react-dom@18.2.0
- react-router-dom@6.20.0

### UI/Visualization
- recharts@2.10.3
- lucide-react@0.292.0
- tailwindcss@3.4.1

### Animation
- framer-motion@10.16.16

### Utils
- date-fns@2.30.0
- clsx@2.0.0
- react-markdown@9.0.1

## License

MIT License - див. LICENSE файл для деталей
